# Example environment variables for docker-compose
# Point the frontend to your backend API base (including port)
# For local development with backend on the host (Docker Desktop):
REACT_APP_API_BASE=http://host.docker.internal:8000

# Or, for a LAN IP where your backend is reachable:
# REACT_APP_API_BASE=http://192.168.0.100:8000
